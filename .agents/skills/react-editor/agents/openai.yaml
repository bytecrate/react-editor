interface:
  display_name: "React Editor"
  short_description: "Repo-specific patterns and workflows for react-editor"
  default_prompt: "Use the react-editor repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true